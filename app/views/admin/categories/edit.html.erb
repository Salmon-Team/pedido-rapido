<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%=t("actions.category.edit")%></h1>

  <%= render "form", category: @category %>

  <%= link_to t("actions.category.show"), @category, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to t("actions.category.back"), categories_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
